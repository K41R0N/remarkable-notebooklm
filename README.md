# remarkable-notebooklm

Sync reMarkable handwritten notes to NotebookLM/Gemini and return AI responses as PDFs.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

See `docs/` for setup guides.
